DB_USER=sa
DB_PASSWORD=your_password
DB_SERVER=localhost
DB_DATABASE=AntigraviPizza
PORT=3000

# Email Configuration (Required for sending guest invitations)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="AntigraviPizza <your-email@gmail.com>"

# Application URL (for generating guest page links in emails)
APP_URL=http://localhost:3000

# Database Type (sqlite or turso)
DB_TYPE=sqlite

# Optional: Hugging Face API token for better image quality
# Get your free token at https://huggingface.co/settings/tokens
# If not configured, the system will use AI Horde and Pollinations as fallback
# HUGGINGFACE_TOKEN=hf_your_token_here
